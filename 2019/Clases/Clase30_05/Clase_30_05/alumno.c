#include "string.h"
#include "alumno.h"
/*
char parseAlumno(sAlumno alumno)
{
    char parsedText[50];
    strcpy(parsedText, alumno.nombre);
    strcat(parsedText,';');
    strcat(parsedText, itoa(alumno.legajo));
    strcat(parsedText,';');
    strcat(parsedText, itoa(alumno.nota));
    strcat(parsedText,'\n');
}
*/
